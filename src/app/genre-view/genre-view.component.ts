import { Component } from '@angular/core';
import { NavbarComponent } from '../navbar/navbar.component';

/**
 * GenreViewComponent is responsible for displaying the genre view of the application.
 * This component is currently not in use but can be populated in the future.
 *
 * @class GenreViewComponent
 */
@Component({
  selector: 'app-genre-view',
  imports: [NavbarComponent],
  templateUrl: './genre-view.component.html',
  styleUrl: './genre-view.component.scss',
})
export class GenreViewComponent {}
